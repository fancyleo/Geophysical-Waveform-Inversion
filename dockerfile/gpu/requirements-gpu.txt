# GPU training container dependency pins
#
# Notes:
# - torch / torchvision / torchaudio are intentionally not listed here: they are
#   reused from the base image gcr.io/kaggle-gpu-images/python (cu121 build) to
#   avoid version drift or reinstalls. To force a version, uncomment and pin it,
#   but make sure it matches the base image's CUDA.
# - The remaining deps are pinned to the same version ranges as the local dev env.
numpy>=2.5.2
pandas>=3.0.5
scikit-learn>=1.9.0
matplotlib>=3.11.1
tqdm>=4.70.0
psutil>=5.9.0
jupyter>=1.1.1
